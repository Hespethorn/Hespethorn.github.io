if theme.aside.card_categories.enable
  if site.categories.length
    .card-widget.card-categories
      !=aside_categories({ limit: 1000, expand: theme.aside.card_categories.expand }) 
      // #theme.aside.card_categories.limit 
